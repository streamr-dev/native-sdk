#include <gtest/gtest.h>

import streamr.trackerlessnetwork.Propagation;

using streamr::trackerlessnetwork::propagation::Propagation; // NOLINT

TEST(PropagationTest, ItCanBeInstantiated) {
    // Propagation propagation;
}
